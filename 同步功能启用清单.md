# 同步功能启用清单（按顺序做）

你的 `firebase-config.js` 已填好，项目 ID：**ux-af71e**。  
**照片存在 Firestore（不用开 Storage、不用升级付费）**，按下面做完即可同步。

---

## 1. 在 Firebase 控制台开启 Firestore（必做）

1. 打开 https://console.firebase.google.com → 选择项目 **ux-af71e**
2. 左侧点 **「Firestore Database」**
3. 若提示「创建数据库」→ 点 **「创建数据库」** → 选 **「在测试模式下启动」** → 选区域（如 `asia-east1`）→ **启用**
4. 若已有数据库，点上方 **「规则」**，把下面整段**全部替换**进去，点 **「发布」**：

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /rooms/{roomId} {
      allow read, write: if true;
      match /photos/{photoId} {
        allow read, write: if true;
      }
    }
  }
}
```

---

## 2. 不需要开 Storage

照片已改为存在 Firestore 里，**不用**再点「Storage」或升级项目，忽略 Storage 相关提示即可。

---

## 3. 在网站里使用同步

**电脑端：**
1. 打开你的纪念站（本地或 Vercel 地址）
2. 页面**顶部**应出现 **「与对象同步」**
3. 你点 **「生成配对码」**，把 6 位码发给对方
4. 对方在顶部输入**同一串码**，点 **「进入」**

**手机端：**
1. 打开网站（Vercel 地址），点底部 **「更多」**
2. 在「更多」页**最上方**有 **「与对象同步」** 卡片
3. 在此输入配对码点「输入配对码进入」，或点「生成配对码」发给对方

配对成功后，相册、目标、留言、纪念日等都会实时同步。

---

## 若顶部没有「与对象同步」

- 确认 `firebase-config.js` 已保存且 `projectId` 是 `ux-af71e`
- 若网站是部署在 Vercel 上的，需要把最新的 `firebase-config.js` 推送到 GitHub，等 Vercel 重新部署后再打开网站

## 若上传照片或保存时报错

- 回到上面 **1**，确认 Firestore 规则已**发布**，且规则里包含 **rooms** 和 **photos 子集合**（与上面给的完全一致）
- **不需要再开 Storage**，照片全部存在 Firestore 里
- 若提示「照片体积过大」，请选择更小或更简单的图片重试
- 若提示「同步未就绪」，请刷新页面后重新配对再上传
